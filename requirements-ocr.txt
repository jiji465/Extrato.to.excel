# OCR para PDFs escaneados (ex.: Caixa) + testes.
# Instalado localmente (iniciar.bat) e no Render. NÃO usar no Vercel.
-r requirements.txt

pymupdf>=1.24.0
rapidocr-onnxruntime>=1.3.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
pillow>=10.0.0

pytest>=8.0.0        # apenas para rodar os testes
