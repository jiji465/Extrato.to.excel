# OCR para PDFs escaneados (ex.: Caixa) — servidor local e Render.
# NÃO usar no Vercel (excede o limite serverless; lá o OCR roda no navegador).
-r requirements.txt

pymupdf>=1.24.0
rapidocr-onnxruntime>=1.3.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
pillow>=10.0.0

# Para rodar os testes, instale à parte: pip install -r requirements-dev.txt
